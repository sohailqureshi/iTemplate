@model IEnumerable<iTemplate.Web.Models.Data.AddressBook>
@{
  Layout = "~/Views/Shared/_Layout.cshtml";
  @PageTitle.Heading("Contact Details")
}
<div class="row">
  <div class="form-group col-md-3">
    <div class="input-group">
      @PageButton.Create()
    </div>
  </div>
</div>
<table class="table">
  <thead>
    <tr>
      <th>
          @Html.DisplayNameFor(model => model.AddressFull)
      </th>
      @*<th>
        @Html.DisplayNameFor(model => model.AddressType.Name)
      </th>
      <th>
          @Html.DisplayNameFor(model => model.Country.Name)
      </th>
      <th>
        @Html.DisplayNameFor(model => model.ContactDetails.FirstOrDefault().ContactType.Name)
      </th>*@
      <th class="text-center">
          @Html.DisplayNameFor(model => model.IsPublished)
      </th>
    </tr>
    </thead>
  <tbody>
  @foreach (var item in Model)
  {
    <tr>
      <td>
        @Html.ActionLink(item.AddressFull, "Edit", new { id = item.Id })
      @*</td>
      <td>
        @Html.DisplayFor(modelItem => item.AddressType.Name)
      </td>
      <td>
        @Html.DisplayFor(modelItem => item.Country.Name)
      </td>
      <td>
        @Html.DisplayFor(modelItem => item.ContactDetails.FirstOrDefault().ContactText)
      </td>*@
      <td class="text-center">
        @Html.CheckBoxFor(modelItem => item.IsPublished)
      </td>
    </tr>
  }
  </tbody>
</table>
